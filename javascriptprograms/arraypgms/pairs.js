var arr=[3,4,1,2]

var sort()
var elem=6;
var low=0,upp=arr.length-1;
while(low<upp){
    let total=arr[low]+arr[upp];

    if(elem==total){
        console.log(`pairs ${arr[low]} ${arr[upp]}`);
        low++;
    }
    else if(elem>total){
        upp
    }
}
